{
  "name": "figma-path-warping-live-raster-plugin",
  "version": "1.2.0",
  "private": true,
  "description": "A Figma plugin that flattens source layers to vectors and live-previews brush warping along Bezier paths.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.119.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
